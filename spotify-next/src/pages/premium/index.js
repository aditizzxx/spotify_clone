import { connect } from "react-redux";
import Premium from "./view";

const mapStateToProps = (state) => {
  return {
    
  };
};

const mapDispatchToProps = {
    
};

export default connect(mapStateToProps, mapDispatchToProps)(Premium);
